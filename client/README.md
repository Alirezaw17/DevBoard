index.css in src: 

  This is already imported in main.tsx by default, so it applies to every page automatically.

   Use it for:

    body background color

    default font

    global resets

    dark theme variables



    